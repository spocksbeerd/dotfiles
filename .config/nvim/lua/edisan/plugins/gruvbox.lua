require("gruvbox").setup({
    transparent_mode = true,
    italic = {
        strings = false,
        comments = true,
        operators = false,
        folds = false,
    },
})

vim.cmd.colorscheme "gruvbox"
